apiVersion: v1
kind: Service
metadata:
  name: postgres-universidad
  namespace: utpl
spec:
  selector:
    app: postgres-universidad
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
