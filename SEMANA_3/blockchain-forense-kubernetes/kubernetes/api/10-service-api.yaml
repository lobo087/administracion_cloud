apiVersion: v1
kind: Service
metadata:
  name: forense-api
  namespace: blockchain-forense
spec:
  type: ClusterIP
  selector:
    app: forense-api
  ports:
    - name: http
      port: 80
      targetPort: http
