{
  "name": "smart-contract-hardhat-docker",
  "version": "1.0.0",
  "description": "Contrato Solidity simple con Hardhat y Docker",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node --hostname 0.0.0.0",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "interact": "hardhat run scripts/interact.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.18"
  }
}
